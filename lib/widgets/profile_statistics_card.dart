import 'package:flutter/material.dart';

class ProfileStatisticsCard extends StatelessWidget {
  const ProfileStatisticsCard({super.key});

  @override
  Widget build(BuildContext context) {
    // Return an empty container or placeholder since the compatibility quiz button
    // has been moved to the main profile tab
    return const SizedBox.shrink();
  }
}
